# 검수 근거와 미완료 범위

## 새 데스크톱 재개 — 2026-09-16

- 복원된 기존 원화57개 SHA-256과 문서 링크 검사 통과. 테스트42개/7파일 및 typecheck 포함 build 재확인.
- 원화 수정5개와 기존20개를 WebP로 반영하고 실제 배포 파일 접촉 시트를 확인했다. [자세한 검수와 한계](RESUME_REVIEW.md).
- 현재 에셋 검사 결과는 output/qa/assets.json을 따른다. 아직 누락이 있으므로 check:assets는 실패하며, 최신 수납장 브라우저·42조합 검수는 아래 재개 후 추가 검수에 기록했다. 실제 기기·교실 리허설은 아직이다.

## 이번 인계에서 다시 확인 — 2026-09-16 오전8시21분 이후 KST

- 테스트42개/7파일 통과. `npm run build`의 typecheck와 Vite 빌드 통과.
- portable source 경로로 `prepare-assets.py` 재실행: 반영 원본30개 처리 통과.
- 원본57개 SHA-256, 위키 링크8문서, 작업 의존 관계10개 검사 통과.
- 원화57/112, public32/121, public누락89개를 구분해 기록. 추가 이미지 때문에 달라진 metrics도 빌드에 포함.
- 이번 인계 작업에서는 브라우저 장면을 다시 조작하지 않았다. 이전 책상 검수 범위는 아래와 같다.
- 전달 ZIP은 `scripts/package-handoff.py`로 만들고 CRC 및 전체 파일 SHA-256을 대조한다. 결과는 `output/transfer/package-report.json`에 기록한다.

## 이미 확인한 범위

- 내 책상42개 추가 선택 조합, 초기 배치·완료 예시·저장 복원, 잘못된 관통/포개기 거부.
- 브라우저: 추가3개/네 번째 제한, 드래그와 목록→놓기, 회전, 가방 걸기/꺼내기, 책 포개기, 잘못된 청소 순서, 퀴즈, 초기화/취소, PNG 저장/끝내기.
- 키보드: Tab/Enter 시작부터 정리 진입, 문항/설명 포커스, Escape 확인창 취소. 캔버스 모든 좌표의 키보드 이동을 검증했다는 뜻은 아니다.
- 추가 이미지 실제 로드 실패/재시도: 자와 물병 파일을 잠시 이동해 확인한 뒤 전부 복구. 선택 유지·복구 후 포커스 확인.
- CSS 화면 크기5개와390×844 흐름 확인. 브라우저 viewport 설정이 적용되지 않아 개발용 iframe으로 검사했으며 실제 기기 검수는 아니다.
- `/`, `/Cleaning/`, `/Cleaning/index.html` 로컬 빌드 경로 확인. 실제 배포는 하지 않았다.
- 바구니32구멍 가림은 수납장 시험 화면에서 확인. 수납장 학생 활동 완료를 뜻하지 않는다.
- PNG 실패(null,빈 파일,MIME,SecurityError,타임아웃,취소)와 캐시/자원 정리는 자동 검사. 모든 브라우저의 실제 실패 화면을 확인한 것은 아니다.

## 보존한 증거

- [책상 결과 PNG](../../output/qa/school-desk-result.png):1984×848. SHA256 `74BD05E2400FDF18F7F5963BAF5F482280026973F4615575E242E19FD4300266`.
- [수납장 가림 PNG](../../output/qa/cabinet-basket-occlusion.png):960×720, 실제 물건 배치 완료 예시 아님.
- [에셋 파일 검사](../../output/qa/assets.json): 검사 실행 때 갱신. 현재 누락이 있으므로 exit1이 맞다.
- 인계 시 재실행 로그는 `output/qa/handoff-*.txt`로 보존한다. 통과 여부는 로그의 exit code와 내용을 확인한다.

## 재실행 명령

```powershell
npm.cmd run handoff:sync
node scripts/write-asset-queue.mjs
npm.cmd run check:handoff
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run check:assets
```

개발 서버 `npm.cmd run dev -- --port 5173`. `?review`는 배치/수납 프레임, `?responsive`는 CSS화면, `?cabinet-review`는 바구니 구멍 검수다. 검수용 코드는 배포 빌드에서 제외한다. `node scripts/serve-dist.mjs`는4173의 로컬 빌드를 제공한다.

## 아직 해야 하는 것

1. 기존 미반영27개와 수납장 상자3개를 반영했다. 아직 없는 원화를 제작한다. 수량은 STATUS 자동 목록을 우선한다.
2. 아직 미생성/미연결인 시점 제작·연결과 나머지5개 실제 공간의 전체 활동.
3. 남은 공간210조합, 입구/선반 천장 충돌, 옷걸이 접점, 신발 밑창, 실제 그림자와 가림.
4. 전체 맵을 오가며 장시간 드래그·캐시·메모리 확인. 모의 캐시5회 검사는 실제5개 공간 성능 검수가 아니다.
5. iOS/Android 등 실제 기기의 터치,가상 키보드,safe-area,회전,다운로드. 교실 PC·프로젝터·교사 리허설.
6. 전체121파일과 최종 썸네일, 필요하다면 별도 승인 범위에서 실제 게시 확인.

수업 준비 완료라는 표현은 이 미완료 범위를 감추지 않을 때만 사용한다. 테스트 통과와 전체 수업 완성은 다르다.

## 재개 후 추가 검수

- 49개 테스트/9파일, typecheck·build 통과. handoff 원화60개 해시·문서9개·작업10개 통과.
- 배포62/121, 누락59개로 check:assets는 예상대로 실패. 잘못된 크기/미사용 파일0.
- ?box-review: 수납장 상자3개 실제 WebP·선반 합성, 화면/저장 픽셀 최대차이0, 내부/외부 알파 클릭 통과. output/qa/cabinet-box-view-review.png 보존. 전체 수납장 활동 검수와 구분한다.
- 수납장42조합: 초기/정리 후 면 내부·밑면 충돌, 모든 실제 MOVE·복원·청소·퀴즈 상태 전이 통과. 공 입구 폭/천장 위반 거절 및 넣기/꺼내기101프레임씩 자동 검사.
- 브라우저84장(42조합×전후)의 화면/PNG 최대 픽셀 차이0. output/qa/cabinet-before-1..7.png와 cabinet-after-1..7.png에 보존. 대표 조합의 접촉·비율·가림을 직접 확인했다.
- 실제 브라우저에서 추가 e1/e5/e6, 물건11개 목록→놓기, 휴지통 입구 클릭, 환기→손걸레→빗자루→바닥걸레→도구 보관→3문항→결과 생성 진행. 결과 이미지 naturalWidth1984/naturalHeight848 확인. 저장 링크 실행 및 브라우저 결과 캡처 output/qa/cabinet-result-browser.png 보존. 다운로드된 파일 위치 자체의 재열기 검증은 별도이며 브라우저의 표시 이미지를 확인한 것이다.
- 새로고침→이어하기로 결과 복원. 초기화 후 공을 바구니에 직접 드래그하고 다시 바닥으로 꺼내는 동작을 확인했다. 실제 터치 기기 검수와는 구분한다.
- 기본8개 공통 렌더러 썸네일 교체, 원본 output/qa/cabinet-base-thumbnail-source.png 보존. 수납장 map revision4, 학생 활동 활성화.

- 마지막 검사: 원본63/112, 배포65/121, 미생성49, 배포 누락56(원화49+썸네일7). 49테스트/9파일·typecheck·build·인계 해시63개 통과. check:assets는 미완성 범위 때문에 exit1이다. 기계 판독 보고서 output/qa/cabinet-review.json.

## 2026-09-16 사물함 추가 검수

- 원화2개 추가: small-pouch, document-folder--shelf. 파우치 원본 알파 보존, 파일은 엄지 홈까지 추적하여 격자를 제거하고 RGB를 보존했다. 밝은/어두운 배경 검수 자료는 output/qa/locker-new-art-contact.png.
- 실제 배경의 책칸3개·준비물 선반5개·주머니 칸/걸이·휴지통을 측정했다.42조합 전후 면/충돌 및 모든 MOVE·복원·청소·퀴즈 전이 통과. 책3종 양방향101프레임씩 전환이 입구 밖에서 끝나고 천장을 넘지 않는지 검사했다.
- 브라우저84장 화면/저장 픽셀 차이0. locker-before-1..7.png, locker-after-1..7.png 및 기본8개 썸네일 원본을 보존했고18개 locker 관련PNG를 실제 디코딩했다.
- 실제 브라우저 추가 e1/e2/e6 조합의11개 목록→놓기, 교과서 책칸→바닥→책칸 드래그, 환기·청소4개·3문항·결과 생성 확인. 결과 이미지1984×848. 다운로드 파일 재열기/실제 터치 기기/교실 리허설은 미검수.
- 사물함 revision4로 활성화, 실제 기본8개 썸네일 합성. 53테스트/10파일·typecheck·build 통과.

## 2026-09-16 도서관 추가 검수

- 책등7개를 각각 원본 표지로 참조 생성했다. 실제 RGBA와 밝은/어두운 합성 검수 후 원본 알파를 그대로 보존했다. library-books-first/second.png.
- 표지 진열대는 경사진 실제 패널, 정보책은 책등 시점, 반납 선반/독서 탁자는 표지 원화를 사용한다. 경사진 패널에는 북엔드 같은 세운 물건을 올릴 수 없다. 책장 천장, 앞턱, 독서 탁자 다리/상판 가림을 측정했다.
-42조합의 전후 면/충돌과 모든 상태 전이, 책3종 양방향101프레임 검사가 통과했다. 브라우저84장 화면/PNG 픽셀 차이0. 전후14장·썸네일 원본·원화 합성2장·결과 캡처 총18PNG 실제 디코딩 완료.
- 실제 브라우저 e1/e3/e5 조합의11개 배치, 과학책 선반→바닥→선반 드래그, 청소4개·퀴즈3개·결과1984×848, 새로고침→이어하기→결과 재생성 확인. 다운로드 파일 재열기와 실제 기기는 별도.
- 도서관 revision4로 활성화, 기본8개 썸네일 합성. 57테스트/11파일·typecheck·build 통과.

## 공부 책상 — 2026-09-16 11:48

- 실제 배경의 왼쪽 책꽂이·뒤 자료 받침·상판·서랍 입구/안쪽 바닥·휴지통을 측정했다.
- 42조합의 전후 지지면/충돌/전체 상태 전이와 처음 각도를 유지한 입구 클릭42조합을 통과했다. 서랍 입구 탭의 안쪽 바닥 투영은 명시적 entryOffsetY가 있는 면에만 적용한다.
- 실제 브라우저에서 추가 가위·테이프·파우치를 포함한11개 정리, 필통 꺼내기/드래그 재수납, 청소4곳,퀴즈3문항,1984×848 결과와 새로고침 후 이어하기를 확인했다.
- 84장 화면/PNG 최대 픽셀 차이0,14개 시트와 기본8개 썸네일 원본을 보존했다. 대표 장면/결과를 시각 검수했으며84개 장면 각각의 세부 수동 검수로 표현하지 않는다.
- typecheck/62테스트/빌드 통과. 실제 다운로드 파일 재열기, 실제 기기·프로젝터·수업 리허설과 배포는 별도 미완료.
- 기록: output/qa/home-desk-review.json, home-desk-result-browser.png, home-desk-drawer-extracted.png.

## 침실 — 2026-09-16 12:01

- 배경에 붙어 있던 베개/이불을 제거한 빈 매트리스 수정본을 반영하고 원본/기존 배포본을 보존했다. 침구4개,책장,옷 바구니 입구/내부 바닥,가방 선반,휴지통을 실측했다.
- 42조합 전후 지지면/충돌/상태 전이와 처음 각도를 유지한 입구 클릭42조합 통과. 브라우저84장 화면/PNG 최대 픽셀 차이0.
- 추가 침대 시트·쿠션·책가방을 포함한11개 실제 배치, 잠옷 꺼내기/드래그 재수납, 청소4곳,퀴즈3개,1984×848 결과와 새로고침 후 이어하기 통과. 대표 침구 장면/결과/꺼내기를 시각 검수했다.
- 67테스트/13파일·typecheck 포함 빌드 통과. 실제 다운로드 파일 재열기·실기기·교실 리허설·배포는 별도 미완료. 기록: output/qa/bedroom-review.json.

## 이동 직전 확인 — 2026-09-16 13:44 KST

원화107개 전체 디코딩·해시/문서 연결 검사 통과. 67테스트/13파일 및 typecheck 포함 빌드 통과. 에셋 검사는98/121로23개 누락 때문에 실패하며 invalid/unused는0이다. 옷장 진행 코드는 컴파일되지만 전용 동작/시각 검수 전이다. 기록: `output/qa/desktop-transfer-review.json`(프로젝트 루트 기준).
