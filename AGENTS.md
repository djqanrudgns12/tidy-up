# 이 프로젝트를 이어서 작업할 때

## 새 세션 시작점

- 사용자가 `이어서 해`라고 하면 먼저 루트의 `handoff.md`를 읽고 그 재개 순서를 따른다.
- 2026-09-16 최신 이동 인계는 `docs/wiki/DESKTOP_HANDOFF_2026-09-16.md`다. 원화107개 보존, 미반영15개와 미생성5개를 구분하고 옷장 걸기 진행 코드를 완료로 간주하지 않는다.
- 최신 구현 상태는 `docs/wiki/STATUS.md`, 실제 에셋 상태는 `docs/wiki/asset-state.json`, 작업 의존 관계는 `docs/wiki/work-tree.json`이 기준이다. 오래된 `TASKS.md`의 시각별 수치나 이미지 한도 오류를 현재 상태로 간주하지 않는다.
- 이번 데스크톱에서는 사용자 요청으로 이미지 제작을 중단하고 인계를 준비했다. 다음 데스크톱에서의 재개 지시는 기존 구현 승인을 이어받는다. 이미 생성된 미검수 원화를 다시 생성하기 전에 로컬 원본과 검수 목록을 확인한다.
- 외부 Codex 대화·생성 폴더·브라우저 상태는 인계의 전제 조건이 아니다. 모든 필수 자료는 프로젝트 내부 상대 경로로 찾는다.

## 사용자 지시 — 2026-09-16

- 구현은 승인되었다. 일정 단축보다 공개수업의 완성도를 우선한다.
- 이후 사용자는 오늘 제작을 마쳐 내일 수업에 사용할 것을 요청했다. 필수 범위와 자연스러움 기준을 유지하면서 의존 관계에 따라 우선순위를 조정한다. 최신 데스크톱 이동 지시와 날짜 해석은 `handoff.md`를 따른다.
- 대상은 초등학교 5학년이고, 학부모와 교사가 수업을 지켜본다. 학교와 집에서 흔히 사용하는 물건과 알아듣기 쉬운 한국어를 쓴다.
- 물건은 실제로 그 자리에 놓인 것처럼 보여야 한다. 밑면·손잡이 접점, 지지면, 원근, 크기, 그림자, 앞뒤 가림을 함께 확인한다.
- 바구니·수납칸에는 입구를 통과해 내려가는 동작이 필요하다. 앞턱에 차례로 가려져야 하며, 임의로 작아지거나 순간 이동해서는 안 된다.
- 눕힘·세움·걸기의 시점이 다르면 필요한 원화를 추가한다. 기존 에셋도 재검수한다. 정면 그림을 단순 회전·늘이기로 다른 시점처럼 만들지 않는다.
- 작은 충돌은 가까운 빈자리로 자연스럽게 밀어 주는 배치 보정을 검토한다. 안정적으로 책을 포개는 동작과 의도하지 않은 관통을 구분한다. 벽 밖으로 밀기, 물건끼리 관통하기, 여러 물건을 자동으로 줄 세우기는 허용하지 않는다.
- 이런 일상적인 작은 동작을 구현·검수 목록에 남기고, 다음 맵에도 같은 기준을 적용한다.

## 구현과 검수

- `PRD.md`, `docs/PLACEMENT_REVIEW.md`, `TASKS.md`를 함께 읽는다. 최신 사용자 지시가 기존 PRD의 단순화 규칙보다 우선한다.
- `src/data/physical.ts`는 실제 배경에서 측정한 좌표다. 계획용 사각형을 실제 가구의 면으로 간주하지 않는다.
- 화면과 최종 PNG는 같은 렌더러를 쓴다. 이동 중 애니메이션을 저장 데이터나 최종 PNG에 섞지 않는다.
- 대표 장면에서 실제 에셋·배치·수납 동작을 먼저 확인한 뒤 다른 공간으로 확장한다. 자동 검사만으로 자연스러움이 확인되었다고 하지 않는다.
- 내 책상은 추가 물건 0~3개를 고르는 42개 조합을 모두 검사한다. 다른 맵도 실제 에셋과 면이 확정되면 같은 검사를 적용한다.
- `npm.cmd run typecheck`, `npm.cmd test`, `npm.cmd run build`를 변경에 맞게 실행한다. 전체 에셋이 없으면 `npm.cmd run check:assets`의 실패를 숨기지 않는다.
- 실제 기기, 공개수업 리허설, 배포 확인과 브라우저 모의 검수를 구분한다.
- 이미지 생성 사용량 한도로 새 원화를 만들 수 없는 경우, 임시 그림으로 완료 처리하지 않는다. 남은 원화·시점과 재검수 항목을 명시한다.
- 원본 PDF·원화와 동시 작업을 보존한다. 명시적 요청 없이 커밋·푸시·배포·공개 저장소 전환을 하지 않는다.
